using System;

namespace _06._Sum_big_numbers
{
    class Program
    {
        static void Main(string[] args)
        {
            string numOne=Console.ReadLine();
            string numTwo=Console.ReadLine();

            int shorterNum=Math.Min(numOne.Length,numTwo.Length);

            for (int i = shorterNum - 1; i >= 0; i--)
            {

            }
        }
    }
}
